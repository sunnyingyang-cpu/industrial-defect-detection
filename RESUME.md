# 简历素材 · 工业表面缺陷检测项目（A 路线）

> 用途：把 `industrial-defect-detection` 这个项目，整理成可直接粘贴进简历 / 面试陈述的素材。
> 原则：**只写事实、不夸大**。带 `（需实测）` 的指标请跑完 `test.py` 再填，避免被追问时露馅。

---

## 一、项目经历条目（直接复制）

**工业表面缺陷检测系统** ｜ 个人项目 ｜ 2026
- 基于 YOLOv5 搭建产线表面缺陷（划痕 / 龟裂 / 夹杂 / 凹坑等）目标检测方案，覆盖数据转换 → 训练 → 评测 → 部署全流程。
- 针对缺陷“小、暗、尺度杂”的痛点，在基线 YOLOv5s 上增加 **P2/4 小目标检测头** 并引入 **C3TR Transformer 注意力** 模块，提升亚像素级小缺陷与低对比度缺陷的召回。
- 提供 NEU-DET 多格式标注（VOC-XML / 原生 txt）一键转 YOLO 格式脚本，自动完成 train/val/test 切分，降低工业数据接入成本。
- 完成 **ONNX / TensorRT** 模型导出并封装 Flask REST 推理服务，打通从算法到产线可部署质检服务的闭环，支持边缘设备（Jetson 类）实时推理。

---

## 二、可挂的技能关键词

- 目标检测：YOLOv5（训练 / 调参 / 自定义 backbone & neck）
- 算法优化：小目标检测（P2 头）、多尺度检测（P6）、Transformer 注意力（C3TR）、anchor 重聚类（autoanchor）
- 模型部署：ONNX / TensorRT / Flask 推理服务 / 边缘设备落地
- 数据工程：标注格式转换、数据集切分、数据增强
- 工程规范：Git / CI（GitHub Actions）/ requirements 依赖管理

---

## 三、面试可能被追问 & 怎么答

**Q1：P2 头具体怎么加的？为什么能提升小目标？**
> 在标准 PANet neck 的 P3/P4/P5 之外，从 backbone 的 P2/4 特征图上采样上采样，新增一个 P2/4 检测分支，让 8× 以下的小目标也能匹配到合适尺度的锚框；Detect 层同时输出 P2–P5 四尺度预测。细节见 `models/defect_yolov5s_p2.yaml`。

**Q2：Transformer 模块用在哪？有没有必要？**
> 用在 P6 版 neck 末端（C3 → C3TR），让高层特征做全局自注意力，增强对“斑块 / 龟裂”这类依赖上下文才能判定的低对比度缺陷的判别力。算力敏感场景可只上 P2 版，不上 Transformer。

**Q3：和你们以前做的业务端 Agent 有什么关系？**
> 分工不同：Agent/RAG 解决“业务逻辑智能化”，本项目解决“产线视觉质检”。两者都属于“把 AI 落到工业现场”的能力栈；真实落地时，缺陷检测结果可作为事件源，触发 Agent 做工单 / 根因分析（可主动提这个结合点，显得有系统视角）。

**Q4：指标多少？在你自己数据上跑过吗？**
> 公开 NEU-DET 上做方法验证；落到公司自有偏振片 / 薄膜缺陷数据时，需要先用 `tools/prepare_neu_det.py` 转标注再训练，指标以实测为准（见 README 第 6 节模板）。—— 诚实说“方法通用、需结合业务数据”，比编数字稳妥。

---

## 四、GitHub 仓库一句话描述（用于 repo 简介 / 简历链接）

> 工业表面缺陷检测：基于 YOLOv5 的微小 & 低对比度缺陷检测方案，支持 P2 小目标头 / Transformer 注意力，并提供 ONNX·TensorRT·REST 部署示例。
